<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/alarm">

    <fragment
        android:id="@+id/alarm"
        android:name="com.example.finalexam.fragments.Alarm"
        android:label="Alarm" >
        <action
            android:id="@+id/action_alarm2_to_timer2"
            app:destination="@id/timer" />
    </fragment>
    <fragment
        android:id="@+id/timer"
        android:name="com.example.finalexam.fragments.Timer"
        android:label="Timer" />
</navigation>